<%= content_for :title do %>
	Editing <%= @genre.name %>
<% end %>

<%= content_for :options do %>
	<%= link_to 'Back', back_url %>
	<%= link_to 'Show', @genre %>
<% end %>

<article class="genre">
	<%= render 'form' %>
</article>
