<%= content_for :title do %>
	Editing <%= @movie.title %>
<% end %>

<%= content_for :options do %>
	<%= link_to 'Back', back_url %>
	<%= link_to 'Show', @movie %>
<% end %>

<article class="<%= @movie.dvd == 1 ? "dvd" : "video" %>">
	<%= render 'form' %>
</article>
