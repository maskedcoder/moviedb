<%= content_for :title do %>
	Editing <%= @actor.name %>
<% end %>

<%= content_for :options do %>
	<%= link_to 'Back', back_url %>
	<%= link_to 'Show', @actor %>
<% end %>

<article class="person">
	<%= render 'form' %>
</article>
